# AI development prompts — 14040336 v1.4.3

## Business request distilled
- The Pubapp Site Admin must be completely separated from the venue-admin area.
- The venue-admin sidebar must not behave like Site Admin belongs to venue operations.
- The user currently sees Site Admin from the provider admin and lands on an effectively empty page because the entrypoint points to the venue registry route instead of the Site Admin dashboard route.
- Fix only the affected scope and do not regress working venue-admin or siteadmin capabilities.

## Implementation prompts
1. Read `codingLessonsLearnt.md` and `changelog.md`.
2. Do not modify unrelated auth, menu, venue, or customer features.
3. In `src/app/admin/layout.tsx`, remove the direct `Site admin` nav item from the venue-admin sidebar.
4. If the user is a superadmin, keep only a clearly separated CTA block that opens `/siteadmin`.
5. Ensure the CTA points to `/siteadmin`, not `/siteadmin/venues`.
6. In `src/app/siteadmin/venues/page.tsx`, make the page clearly part of the separate Site Admin shell and add a visible CTA back to `/siteadmin`.
7. Append a new changelog entry; do not replace prior entries.
8. Append a new coding lessons entry describing the route-separation mistake.
9. Deliver patch-only files.
